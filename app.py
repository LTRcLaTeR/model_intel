import streamlit as st

st.set_page_config(page_title="Model Intel", page_icon="🤖")

st.title("🤖 Model Intel")
st.write("เลือกโมเดลจากเมนูด้านซ้าย")

st.markdown("""
### โมเดลที่มี:
- 🎨 **Card Color AI** — ทำนายสีคอสต์(สี่เหลี่ยมุมขวาบน)การ์ดจากรูปภาพ
- 📷 **Camera Price Predictor** — ทำนายราคากล้อง Sony
""")